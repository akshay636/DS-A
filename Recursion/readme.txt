What is recursion ?
A function calling itself is called recursion, the recursion basically used to solve complex problem by divieding 
into smaller part

recursion has three component
Base case -->> this define when recursion should stop
small calculation ---->> define the calculation of function.
recursive case ---->> this is where recursion called, until it mat with it's base case